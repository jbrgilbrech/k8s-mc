apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Keep all resources together so "kubectl apply -k k8s" is enough
# to build and apply the full Minecraft stack.
resources:
  - namespace.yaml
  - pvc.yaml
  - service.yaml
  - statefulset.yaml
